wb = xlsx_package.workbook
wb.add_worksheet(name: "Scores") do |sheet|
  @users.each do |user|
    sheet.add_row [user.name, user.last_names, user.campus, user.enrollment, user.major, user.score]
  end
end